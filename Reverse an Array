class Solution {
    public void reverseArray(int arr[]) {
        // code here
        int n = arr.length;
        for(int i = 0; i <= 0; i++){
           int temp = arr[i];
           arr[i]=arr[n-1];
           arr[n-1]=temp;
           n=n-1;
        }
    }
}
